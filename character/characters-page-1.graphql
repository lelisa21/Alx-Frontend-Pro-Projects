query GetCharacters {
  characters(page: 1) {
    info {
      count
      pages
      next
      prev
    }
    results {
      id
      name
      status
      image
    }
  }
}

query GetEpisode {
  episode(id: 1) {
    id
    name
    air_date
    episode
  }
}
